import React from 'react'
import { Button } from '@chakra-ui/react'

type Props = {}

export default function AddTask() {
  return (
    <Button  margin={4} colorScheme='teal'> Add New Task </Button>
  )
}